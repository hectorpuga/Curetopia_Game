import 'package:flutter/cupertino.dart';

class DialogOverlay extends StatelessWidget {
  const DialogOverlay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Dialgo goes");
  }
}
